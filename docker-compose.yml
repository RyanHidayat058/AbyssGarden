services:
  abyss-garden:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: abyss-garden
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
